[Desktop Entry]
# The version of the desktop entry spec this conforms to.
Version=1.0

Type=Application

Name=OpenWarp
GenericName=TerminalEmulator

# `openwarp` 在 deb/rpm 包里是 /opt/openwarp/openwarp-oss 的 symlink,
# 在 arch 包里是 /usr/bin/openwarp wrapper script —— 三者都在 PATH 上能直接调用。
# AppImage 打包时 bundle_appimage 会用 sed 把这一行替换为 `Exec=openwarp-oss %U`,
# 直接指向 AppImage 内部的二进制。
Exec=openwarp %U
StartupWMClass=dev.openwarp.OpenWarp

Keywords=shell;prompt;command;commandline;cmd;

Icon=dev.openwarp.OpenWarp

Categories=System;TerminalEmulator;

# Don't run this application within a terminal.
Terminal=false

# Register ourselves as the handler for openwarp:// URLs.
MimeType=x-scheme-handler/openwarp;
